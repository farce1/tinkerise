---
"@tinkerise/shared": minor
"@tinkerise/core": minor
---

Add `buildStackCommand` — a pure serializer that turns a stack selection into the copy-paste
`tinkerise …` command(s), and export `SCAFFOLD_FLAG_NAMES` (the canonical boolean scaffold flags).
These power the new interactive Stack Builder page in the docs site.
