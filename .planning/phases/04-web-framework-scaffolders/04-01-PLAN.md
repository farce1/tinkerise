---
phase: 04-web-framework-scaffolders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/registry/scaffolders/web.ts
  - packages/core/src/registry/index.ts
  - packages/cli/src/prompts/options-select.ts
  - packages/core/tests/registry/web-scaffolders.test.ts
  - packages/core/tests/flags/web-flag-mapping.test.ts
autonomous: true
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-05, WEB-06, WEB-07, FLAG-01, FLAG-02, FLAG-03, FLAG-04, FLAG-05, FLAG-06]

must_haves:
  truths:
    - "All 7 web scaffolders are registered and resolvable by name"
    - "getScaffoldersByCategory('web') returns all 7 web entries (6 web + turbo in web category)"
    - "Each scaffolder's flag mappings correctly translate unified flags to native upstream flags"
    - "Unsupported unified flags throw FlagNotApplicableError for each scaffolder"
    - "FRAMEWORK_OPTIONS defines selectable options for all scaffolders that support them"
  artifacts:
    - path: "packages/core/src/registry/scaffolders/web.ts"
      provides: "All 7 scaffolder defineScaffolder() entries"
      contains: "defineScaffolder"
    - path: "packages/core/src/registry/index.ts"
      provides: "Registry importing and registering all 7 entries"
      contains: "register("
    - path: "packages/cli/src/prompts/options-select.ts"
      provides: "FRAMEWORK_OPTIONS for all scaffolders with interactive options"
      contains: "FRAMEWORK_OPTIONS"
    - path: "packages/core/tests/registry/web-scaffolders.test.ts"
      provides: "Registry lookup tests for all 7 scaffolders"
    - path: "packages/core/tests/flags/web-flag-mapping.test.ts"
      provides: "Flag mapping and validation tests for all 7 scaffolders"
  key_links:
    - from: "packages/core/src/registry/scaffolders/web.ts"
      to: "packages/core/src/registry/index.ts"
      via: "static import and register() call"
      pattern: "import.*from.*scaffolders/web"
    - from: "packages/core/src/registry/index.ts"
      to: "packages/core/src/flags/resolver.ts"
      via: "flag resolver reads entry.flags from registry"
      pattern: "entry\\.flags"
---

<objective>
Register all 7 web framework scaffolders in the registry with correct flag mappings, update Next.js flags per research, and extend FRAMEWORK_OPTIONS for interactive prompts.

Purpose: This is the data foundation for Phase 4. Every scaffolder is a pure `defineScaffolder()` data entry — no logic changes to the pipeline. The flag mappings encode the research findings (upstream flag names, camelCase vs kebab-case, silent/no-op flags, Astro --add pattern).

Output: 7 registered scaffolders, flag mapping tests validating correctness, options-select extended.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-web-framework-scaffolders/04-RESEARCH.md
@packages/core/src/registry/scaffolders/web.ts
@packages/core/src/registry/index.ts
@packages/shared/src/registry/schemas.ts
@packages/shared/src/registry/types.ts
@packages/core/src/flags/resolver.ts
@packages/core/src/flags/validator.ts
@packages/cli/src/prompts/options-select.ts
@packages/core/tests/flags/resolver.test.ts
@packages/core/tests/flags/validator.test.ts
@packages/core/tests/registry/registry.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all 7 scaffolder registry entries and register them</name>
  <files>
    packages/core/src/registry/scaffolders/web.ts
    packages/core/src/registry/index.ts
    packages/cli/src/prompts/options-select.ts
  </files>
  <action>
**In `packages/core/src/registry/scaffolders/web.ts`:**

1. **Update the existing `nextjs` entry** per research findings:
   - Update the `>=15.0.0` versionedFlags: change `--skip-git` to `--disable-git` (Next.js 15+ changed this flag name)
   - Add `package-manager` flag with `valueMap` and prefix-style native: `'--use-'` mapping `{ npm: 'npm', pnpm: 'pnpm', yarn: 'yarn', bun: 'bun' }`

2. **Add Vite scaffolder (`vite`)**:
   - `name: 'vite'`, `category: 'web'`, `command: 'npx'`, `packageName: 'create-vite'`
   - `integration: { type: 'delegate', command: 'create-vite' }`
   - Prerequisites: `node >=18.0.0` (same install instructions pattern as Next.js)
   - Flags: Only `typescript` with `native: '--template'` — BUT this is special: Vite's typescript handling is done via template suffix, NOT via the flag resolver. So do NOT add typescript to flags array. Instead, leave flags array empty (Vite has no unified flags that map 1:1). The variant selection in Plan 04-02 handles the template+TS merging.
   - Actually, per the locked decision "Passing an unsupported flag is an error" — flags like `--tailwind`, `--eslint`, `--no-git`, `--no-install`, `--package-manager` MUST NOT be in the flags array so they error via `validateFlagApplicability()`. But `--typescript` SHOULD be accepted (not error) because tinkerise handles it specially via template suffix. So add `typescript` with `native: ''` (empty string sentinel — the resolver will produce empty arg which gets filtered; the actual mapping is handled in the CLI layer). This allows validateFlagApplicability to pass for --typescript.
   - flags: `[{ unified: 'typescript', native: '' }]` — empty native means "accepted but handled elsewhere"
   - `passthroughArgs: true`

3. **Add Astro scaffolder (`astro`)**:
   - `name: 'astro'`, `category: 'web'`, `command: 'npx'`, `packageName: 'create-astro'`
   - `integration: { type: 'delegate', command: 'create-astro' }`
   - Prerequisites: `node >=18.17.1`
   - Flags:
     - `{ unified: 'typescript', native: '' }` — silent accept (Astro 5 always uses strict TS)
     - `{ unified: 'tailwind', native: '--add', valueMap: { true: 'tailwindcss' } }` — maps `--tailwind` to `--add tailwindcss`. NOTE: The resolver already handles string valueMap values — when userFlags has `tailwind: true`, the value `true` needs to be looked up as string key `'true'` in the valueMap. Wait — the resolver checks `typeof userValue === 'string' && flagDef.valueMap` but tailwind is a boolean flag. The resolver handles `userValue === true` by pushing `flagDef.native` only. This doesn't work for Astro's `--add tailwindcss` pattern with a boolean input. **Solution:** Use the `valueMap` approach but the boolean `true` won't match the resolver's current code path. Instead, handle this in the executor/CLI layer as a pre-processing step. For the registry entry, just mark tailwind as supported: `{ unified: 'tailwind', native: '--add tailwindcss' }` — push the whole string as a single arg. Wait, that also won't work because it becomes one arg `--add tailwindcss` instead of two args `--add` `tailwindcss`. **Better solution:** Since the flag resolver pushes `flagDef.native` when `userValue === true`, we need the native string to produce the correct args. The simplest approach that works with the existing resolver: don't use the resolver for this flag. Instead, handle Astro's `--tailwind` -> `--add tailwindcss` in a pre-processing step in the executor. For now, register it as `{ unified: 'tailwind', native: '--add' }` — the resolver will push `--add` when tailwind is true. Then in the executor's `buildCommandArgs`, detect Astro + tailwind and append `tailwindcss`. Actually, that couples the executor to Astro-specific logic, which violates the declarative principle.

     **FINAL APPROACH for Astro tailwind:** The simplest correct approach within the existing architecture: modify the resolver to handle the case where `native` contains a space — split on space and push multiple args. So `native: '--add tailwindcss'` produces args `['--add', 'tailwindcss']`. This is a tiny, backward-compatible change to `resolver.ts` (split `flagDef.native` on spaces before pushing). Make this change in `resolver.ts`: when pushing `flagDef.native` for a boolean true value, split on whitespace: `args.push(...flagDef.native.split(/\s+/))` instead of `args.push(flagDef.native)`. This handles both single-arg flags (no space = same behavior) and multi-arg patterns (Astro's `--add tailwindcss`).

     - `{ unified: 'tailwind', native: '--add tailwindcss' }` — split-on-space handled by resolver
     - `{ unified: 'no-git', native: '--no-git' }`
     - `{ unified: 'no-install', native: '--no-install' }`
   - `passthroughArgs: true`

4. **Add T3 scaffolder (`t3`)**:
   - `name: 't3'`, `category: 'web'`, `command: 'npx'`, `packageName: 'create-t3-app'`
   - `integration: { type: 'delegate', command: 'create-t3-app' }`
   - Prerequisites: `node >=18.17.0`
   - Flags (note camelCase native flags per research — Pitfall 3):
     - `{ unified: 'typescript', native: '' }` — silent accept (T3 is TypeScript-only)
     - `{ unified: 'tailwind', native: '--tailwind' }`
     - `{ unified: 'eslint', native: '' }` — silent accept (T3 always includes ESLint)
     - `{ unified: 'no-git', native: '--noGit' }` — camelCase!
     - `{ unified: 'no-install', native: '--noInstall' }` — camelCase!
   - `passthroughArgs: true`

5. **Add Remix/React Router scaffolder (`remix`)**:
   - `name: 'remix'`, `category: 'web'`, `command: 'npx'`, `packageName: 'create-react-router'`
   - `integration: { type: 'delegate', command: 'create-react-router' }`
   - Prerequisites: `node >=18.0.0`
   - Flags (per research — Pitfall 1: use create-react-router, NOT create-remix):
     - `{ unified: 'typescript', native: '' }` — silent accept (RR v7 is TS by default)
     - `{ unified: 'no-git', native: '--no-git-init' }` — note the unique flag name
     - `{ unified: 'no-install', native: '--no-install' }`
   - No tailwind, eslint, or package-manager flags (error if passed)
   - `passthroughArgs: true`

6. **Add TanStack Start scaffolder (`tanstack`)**:
   - `name: 'tanstack'`, `category: 'web'`, `command: 'npx'`, `packageName: '@tanstack/cli'`
   - `integration: { type: 'delegate', command: '@tanstack/cli' }` — the command is `npx @tanstack/cli create`; BUT the delegate strategy builds args as `[command, projectName, ...flags]` which gives `@tanstack/cli my-app`. We need `@tanstack/cli create my-app`. **Solution:** Use `integration: { type: 'wrap', command: '@tanstack/cli' }` — wrap strategy also builds `[command, projectName, ...flags]`. Neither handles the `create` subcommand. The cleanest approach: use the delegate pattern with `command: '@tanstack/cli'` and insert `create` as the first element. Actually, looking at buildCommandArgs: delegate produces `[integration.command, projectName, ...nativeArgs]`. So it would be `['@tanstack/cli', 'my-app', ...flags]` — missing `create`. We need `['@tanstack/cli', 'create', 'my-app', ...flags]`. **Solution:** Set `integration.command` to `'@tanstack/cli create'` — BUT buildCommandArgs doesn't split this. The simplest correct approach: we'll need to slightly modify buildCommandArgs or use a different approach. Actually, looking at the execa call: `execa(entry.command, commandArgs)` — entry.command is 'npx'. So the full command becomes: `npx @tanstack/cli create my-app --tailwind`. The commandArgs just need to be `['@tanstack/cli', 'create', 'my-app', '--tailwind']`. With delegate strategy: `[integration.command, projectName, ...flags]` = `['@tanstack/cli', 'my-app', '--tailwind']` — missing `create`. **Best approach that respects existing architecture:** Embed `create` into the integration command: `integration: { type: 'delegate', command: '@tanstack/cli create' }`. Then in buildCommandArgs, split integration.command on spaces just like we're doing for resolver native flags. Apply the same split-on-space treatment: `args.push(...entry.integration.command.split(/\s+/), projectName, ...nativeArgs)`. This change to buildCommandArgs is small and backward-compatible (existing single-word commands have no spaces, so splitting is a no-op).
   - Prerequisites: `node >=18.0.0`
   - Flags:
     - `{ unified: 'typescript', native: '' }` — silent accept (TS only)
     - `{ unified: 'tailwind', native: '--tailwind' }`
     - `{ unified: 'package-manager', native: '--package-manager' }`
   - No eslint, no-git, no-install flags (error if passed per research)
   - `passthroughArgs: true`

7. **Add Turborepo scaffolder (`turbo`)**:
   - `name: 'turbo'`, `category: 'web'`, `command: 'npx'`, `packageName: 'create-turbo'`
   - Note: category is 'web' in registry for data purposes per research recommendation; CLI routes it via `tinkerise monorepo` (handled in Plan 04-03)
   - `integration: { type: 'delegate', command: 'create-turbo' }`
   - Prerequisites: `node >=18.0.0`
   - Flags:
     - `{ unified: 'no-install', native: '--skip-install' }`
     - `{ unified: 'package-manager', native: '-m' }`
   - No typescript, tailwind, eslint, no-git flags (error if passed)
   - `passthroughArgs: true`

**In `packages/core/src/registry/index.ts`:**
- Import all new entries from `./scaffolders/web.js`: `import { nextjs, vite, astro, t3, remix, tanstack, turbo } from './scaffolders/web.js'`
- Update `register()` call: `register(nextjs, vite, astro, t3, remix, tanstack, turbo)`

**In `packages/core/src/flags/resolver.ts`:**
- In the `resolveFlags` function, where `userValue === true`, change `args.push(flagDef.native)` to handle multi-word native flags: `if (flagDef.native) args.push(...flagDef.native.split(/\s+/))`. This handles Astro's `--add tailwindcss` pattern. The `if (flagDef.native)` guard also handles the empty string sentinel (no-op for silent flags like Astro's typescript). Similarly, add the `if (flagDef.native)` guard for the `nativeDisable` path.
- Also update the `else if (typeof userValue === 'string')` path to split: `args.push(...flagDef.native.split(/\s+/), userValue)`

**In `packages/core/src/executor/index.ts`:**
- In `buildCommandArgs`, for delegate and wrap cases, split the integration command on spaces: `args.push(...entry.integration.command.split(/\s+/), projectName, ...nativeArgs)`. This handles TanStack's `@tanstack/cli create` command pattern.

**In `packages/cli/src/prompts/options-select.ts`:**
- Extend `FRAMEWORK_OPTIONS` with entries for all scaffolders that have interactive options:
  - `next`: already exists (typescript, tailwind, eslint)
  - `vite`: no framework-level options here (template selection is handled separately in Plan 04-02)
  - `astro`: `[{ value: 'tailwind', label: 'Tailwind CSS' }]`
  - `t3`: `[{ value: 'tailwind', label: 'Tailwind CSS' }]` — T3 component selection (trpc, prisma, etc.) is handled via variant prompts in Plan 04-02
  - `remix`: no options (no applicable unified flags beyond silent TS)
  - `tanstack`: `[{ value: 'tailwind', label: 'Tailwind CSS' }]`
  - `turbo`: no options (only no-install and package-manager, which are global flags not multiselect options)
  </action>
  <verify>
- `bun run typecheck` passes across all packages
- `bun run test` — existing tests still pass (no regressions)
- Manual verification: `getScaffolder('vite')` returns the Vite entry, `getScaffoldersByCategory('web')` returns 7 entries
  </verify>
  <done>
- 7 scaffolder entries registered (Next.js updated, 6 new)
- Flag resolver handles multi-word native flags (space-split)
- buildCommandArgs handles multi-word integration commands (space-split)
- FRAMEWORK_OPTIONS extended for Astro, T3, TanStack
- Typecheck passes, existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for all 7 scaffolder registry entries and flag mappings</name>
  <files>
    packages/core/tests/registry/web-scaffolders.test.ts
    packages/core/tests/flags/web-flag-mapping.test.ts
  </files>
  <action>
**Create `packages/core/tests/registry/web-scaffolders.test.ts`:**

Test all 7 scaffolders are registered and resolvable:

```typescript
import { describe, it, expect } from 'vitest'
import { getScaffolder, getAllScaffolders, getScaffoldersByCategory } from '../../src/registry/index'

const WEB_SCAFFOLDERS = ['next', 'vite', 'astro', 't3', 'remix', 'tanstack', 'turbo']

describe('web scaffolder registry', () => {
  for (const name of WEB_SCAFFOLDERS) {
    it(`resolves '${name}' by name`, () => {
      const entry = getScaffolder(name)
      expect(entry).toBeDefined()
      expect(entry!.name).toBe(name)
      expect(entry!.category).toBe('web')
    })
  }

  it('getScaffoldersByCategory("web") returns all 7 entries', () => {
    const entries = getScaffoldersByCategory('web')
    expect(entries).toHaveLength(7)
    const names = entries.map(e => e.name).sort()
    expect(names).toEqual(WEB_SCAFFOLDERS.sort())
  })

  it('getAllScaffolders() includes all 7 web entries', () => {
    const all = getAllScaffolders()
    expect(all.length).toBeGreaterThanOrEqual(7)
  })

  // Verify specific critical fields
  it('remix uses create-react-router, not create-remix', () => {
    const entry = getScaffolder('remix')!
    expect(entry.packageName).toBe('create-react-router')
    expect(entry.integration).toEqual({ type: 'delegate', command: 'create-react-router' })
  })

  it('tanstack uses @tanstack/cli create', () => {
    const entry = getScaffolder('tanstack')!
    expect(entry.packageName).toBe('@tanstack/cli')
    expect(entry.integration).toEqual({ type: 'delegate', command: '@tanstack/cli create' })
  })

  it('t3 uses camelCase flags (--noGit, --noInstall)', () => {
    const entry = getScaffolder('t3')!
    const noGitFlag = entry.flags.find(f => f.unified === 'no-git')
    expect(noGitFlag?.native).toBe('--noGit')
    const noInstallFlag = entry.flags.find(f => f.unified === 'no-install')
    expect(noInstallFlag?.native).toBe('--noInstall')
  })
})
```

**Create `packages/core/tests/flags/web-flag-mapping.test.ts`:**

Test flag resolution and validation for each scaffolder using the real `resolveFlags()` and `validateFlagApplicability()`:

For each scaffolder, test:
1. **Supported flags resolve correctly** — e.g., Next.js `typescript: true` → `['--typescript']`
2. **Unsupported flags throw FlagNotApplicableError** — e.g., Vite + `tailwind: true` → throws
3. **Multi-word native flags split correctly** — e.g., Astro `tailwind: true` → `['--add', 'tailwindcss']`
4. **Silent/no-op flags produce no args** — e.g., Astro `typescript: true` → `[]` (empty native filtered)
5. **T3 camelCase flag names** — `no-git: true` → `['--noGit']`
6. **Next.js versioned flags** — with upstream version `15.1.0`, `no-git: true` → `['--disable-git']`
7. **Next.js package-manager prefix-style** — `'package-manager': 'pnpm'` → `['--use-pnpm']`
8. **Turbo package-manager short flag** — `'package-manager': 'pnpm'` → `['-m', 'pnpm']`
9. **Remix no-git-init** — `no-git: true` → `['--no-git-init']`

Test flag validation (FlagNotApplicableError) for:
- Vite: tailwind, eslint, no-git, no-install, package-manager should all throw
- Remix: tailwind, eslint, package-manager should throw
- TanStack: eslint, no-git, no-install should throw
- Turbo: typescript, tailwind, eslint, no-git should throw

Target: ~25-30 tests covering the flag applicability matrix from the research.
  </action>
  <verify>
- `bun run test` — all new tests pass
- `bun run test -- --reporter=verbose` shows test names matching the matrix
- Total test count increases by ~25-30
  </verify>
  <done>
- Registry lookup tests verify all 7 scaffolders are registered and critical fields are correct
- Flag mapping tests verify correct resolution for every scaffolder + unified flag combination
- Flag validation tests verify FlagNotApplicableError for every unsupported flag + scaffolder combination
- All tests pass, no regressions
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run test` passes (all existing + new tests)
- `getScaffolder('vite')`, `getScaffolder('astro')`, etc. all return valid entries
- `getScaffoldersByCategory('web')` returns 7 entries
- Flag resolver correctly splits multi-word native flags
- buildCommandArgs correctly splits multi-word integration commands
</verification>

<success_criteria>
- 7 scaffolders registered in the registry with correct flag mappings per research
- Flag resolver handles multi-word native flags and silent (empty) flags
- buildCommandArgs handles multi-word integration commands (TanStack)
- ~25-30 new tests covering registry entries and flag mapping matrix
- All tests pass, typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-framework-scaffolders/04-01-SUMMARY.md`
</output>
