---
phase: 09-additional-enhancements-utility-templates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enhancements/modules/commitlint.ts
  - packages/core/src/enhancements/modules/testing.ts
  - packages/core/tests/enhancements/modules/commitlint.test.ts
  - packages/core/tests/enhancements/modules/testing.test.ts
autonomous: true
requirements: [ADD-07, ADD-08]

must_haves:
  truths:
    - "Commitlint module installs @commitlint/cli + config-conventional and generates commitlint.config.js"
    - "Commitlint module adds commit-msg hook only when husky/.husky directory already exists"
    - "Testing module generates vitest.config.ts and adds test scripts to package.json"
    - "Testing module does NOT generate example test files (config only per locked decision)"
  artifacts:
    - path: "packages/core/src/enhancements/modules/commitlint.ts"
      provides: "Commitlint enhancement module"
      exports: ["commitlintModule"]
    - path: "packages/core/src/enhancements/modules/testing.ts"
      provides: "Vitest testing enhancement module"
      exports: ["testingModule"]
    - path: "packages/core/tests/enhancements/modules/commitlint.test.ts"
      provides: "Commitlint module unit tests"
      contains: "commitlintModule"
    - path: "packages/core/tests/enhancements/modules/testing.test.ts"
      provides: "Testing module unit tests"
      contains: "testingModule"
  key_links:
    - from: "packages/core/src/enhancements/modules/commitlint.ts"
      to: "packages/core/src/enhancements/version-map.ts"
      via: "imports dependencyVersionMap for @commitlint/cli and config-conventional versions"
      pattern: "dependencyVersionMap"
    - from: "packages/core/src/enhancements/modules/testing.ts"
      to: "packages/core/src/enhancements/version-map.ts"
      via: "imports dependencyVersionMap for vitest version"
      pattern: "dependencyVersionMap"
---

<objective>
Implement Commitlint and Testing (Vitest) enhancement modules.

Purpose: Enable `tinkerise add commitlint` for conventional commit enforcement and `tinkerise add testing` for Vitest configuration.

Output: Two new enhancement modules with detect/install implementations and unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/enhancements/modules/husky.ts
@packages/core/src/enhancements/modules/_utils.ts
@packages/core/src/enhancements/types.ts
@packages/core/src/enhancements/version-map.ts
@packages/core/tests/enhancements/modules/husky.test.ts
@.planning/phases/09-additional-enhancements-utility-templates/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commitlint enhancement module with husky integration</name>
  <files>
    packages/core/src/enhancements/modules/commitlint.ts
    packages/core/tests/enhancements/modules/commitlint.test.ts
  </files>
  <action>
Create `commitlint.ts` using `defineEnhancement()`.

**detect()**: Check for existing commitlint config files:
- `commitlint.config.js`, `commitlint.config.mjs`, `commitlint.config.cjs`, `commitlint.config.ts`
- `.commitlintrc`, `.commitlintrc.json`, `.commitlintrc.yaml`, `.commitlintrc.yml`, `.commitlintrc.js`
Also check for `commitlint` key in package.json.
Return `{ installed: true }` if any found.

**install()**:
1. Install packages using `installPackages()`: `@commitlint/cli` and `@commitlint/config-conventional` from dependencyVersionMap (versions already exist: `^19.6.0`)
2. Generate `commitlint.config.js` (or `.mjs` if package.json type is not 'module'):
```javascript
export default { extends: ['@commitlint/config-conventional'] };
```
Use `writeConfigFile()` to write.

3. **Husky integration** (per locked decision: "Integrate with husky if already present"):
   - Check if `.husky` directory exists using `access()`
   - If YES: write `.husky/commit-msg` hook file with content: `npx --no -- commitlint --edit $1\n`
   - If NO: do NOT install husky. Add a warning to the result: "Add husky (`tinkerise add husky`) to enable git hook enforcement"
   Per pitfall #2: never silently install husky as a side effect.

4. Return InstallResult with appropriate filesModified and warnings.

**Test file**: Follow husky.test.ts pattern. Mock fs/promises and execa. Test:
- detect returns false when no config files exist
- detect returns true when commitlint.config.js exists
- detect returns true when package.json has commitlint key
- install installs @commitlint/cli and @commitlint/config-conventional
- install generates commitlint.config.js with config-conventional extends
- install uses .mjs extension when package.json type is not 'module'
- install adds commit-msg hook when .husky directory exists
- install does NOT write commit-msg hook when .husky directory is absent
- install includes warning message when husky not present
  </action>
  <verify>Run `bun run test --filter @tinkerise/core` and verify commitlint.test.ts passes.</verify>
  <done>Commitlint module installs conventional commit linting with config-conventional ruleset and conditionally integrates with husky. Warning shown when husky is not present. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Testing (Vitest) enhancement module — config only</name>
  <files>
    packages/core/src/enhancements/modules/testing.ts
    packages/core/tests/enhancements/modules/testing.test.ts
  </files>
  <action>
Create `testing.ts` using `defineEnhancement()`.

**detect()**: Check for existing test config files:
- `vitest.config.ts`, `vitest.config.js`, `vitest.config.mts`, `vitest.config.mjs`
- `jest.config.js`, `jest.config.ts`, `jest.config.mjs`, `jest.config.cjs`
Also check for `vitest` or `jest` in installedDeps.
Return `{ installed: true }` if any found.

**install()** (per locked decisions: "Always Vitest", "Config only", "No example test files"):
1. Install `vitest` using `installPackages()` from dependencyVersionMap (version `^3.1.0` already exists)
2. Generate `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    include: ['**/*.{test,spec}.{js,ts,jsx,tsx}'],
    exclude: ['node_modules', 'dist'],
  },
})
```
Use `writeConfigFile()`.

3. Add scripts to package.json using `addScript()`:
   - `"test": "vitest"`
   - `"test:run": "vitest run"`

4. Do NOT create any example test files (per locked decision: "no example test files").

**Test file**: Follow ci.test.ts pattern. Mock fs/promises and execa. Test:
- detect returns false when no test config exists
- detect returns true when vitest.config.ts exists
- detect returns true when jest is in installedDeps
- install installs vitest package
- install generates vitest.config.ts with defineConfig
- install adds test and test:run scripts to package.json
- install does NOT create any test files (verify no writes to paths containing '.test.' or '.spec.')
  </action>
  <verify>Run `bun run test --filter @tinkerise/core` and verify testing.test.ts passes.</verify>
  <done>Testing module generates vitest.config.ts and adds test scripts. No example test files created. Always Vitest regardless of framework. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun run test --filter @tinkerise/core` passes with all new tests
- Commitlint module correctly handles husky-present vs husky-absent cases
- Testing module generates config only (no example test files)
- Both modules use versions from dependencyVersionMap
- `bun run build` succeeds
</verification>

<success_criteria>
- commitlintModule and testingModule exported with correct IDs
- Commitlint generates config with @commitlint/config-conventional
- Commitlint adds commit-msg hook only when .husky exists, warns otherwise
- Testing generates vitest.config.ts + test scripts, no example files
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-additional-enhancements-utility-templates/09-02-SUMMARY.md`
</output>
