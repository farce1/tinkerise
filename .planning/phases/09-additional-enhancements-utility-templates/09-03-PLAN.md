---
phase: 09-additional-enhancements-utility-templates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/enhancements/modules/renovate.ts
  - packages/core/src/enhancements/modules/editorconfig.ts
  - packages/core/tests/enhancements/modules/renovate.test.ts
  - packages/core/tests/enhancements/modules/editorconfig.test.ts
autonomous: true
requirements: [ADD-09, ADD-10]

must_haves:
  truths:
    - "Renovate module generates renovate.json with config:recommended preset"
    - "EditorConfig module generates .editorconfig with standard JS/TS defaults"
    - "Both are config-only modules — no package installations"
  artifacts:
    - path: "packages/core/src/enhancements/modules/renovate.ts"
      provides: "Renovate enhancement module"
      exports: ["renovateModule"]
    - path: "packages/core/src/enhancements/modules/editorconfig.ts"
      provides: "EditorConfig enhancement module"
      exports: ["editorconfigModule"]
    - path: "packages/core/tests/enhancements/modules/renovate.test.ts"
      provides: "Renovate module unit tests"
      contains: "renovateModule"
    - path: "packages/core/tests/enhancements/modules/editorconfig.test.ts"
      provides: "EditorConfig module unit tests"
      contains: "editorconfigModule"
  key_links:
    - from: "packages/core/src/enhancements/modules/renovate.ts"
      to: "packages/core/src/enhancements/define.ts"
      via: "uses defineEnhancement()"
      pattern: "defineEnhancement"
    - from: "packages/core/src/enhancements/modules/editorconfig.ts"
      to: "packages/core/src/enhancements/define.ts"
      via: "uses defineEnhancement()"
      pattern: "defineEnhancement"
---

<objective>
Implement Renovate and EditorConfig enhancement modules — both lightweight config-file-only modules.

Purpose: Enable `tinkerise add renovate` for automated dependency updates and `tinkerise add editorconfig` for cross-editor formatting consistency.

Output: Two config-only enhancement modules with detect/install implementations and unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/enhancements/modules/ci.ts
@packages/core/src/enhancements/modules/_utils.ts
@packages/core/src/enhancements/types.ts
@packages/core/tests/enhancements/modules/ci.test.ts
@.planning/phases/09-additional-enhancements-utility-templates/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Renovate and EditorConfig enhancement modules</name>
  <files>
    packages/core/src/enhancements/modules/renovate.ts
    packages/core/src/enhancements/modules/editorconfig.ts
    packages/core/tests/enhancements/modules/renovate.test.ts
    packages/core/tests/enhancements/modules/editorconfig.test.ts
  </files>
  <action>
**renovate.ts**: Create using `defineEnhancement()`.

detect(): Check for `renovate.json`, `renovate.json5`, `.renovaterc`, `.renovaterc.json`, `.github/renovate.json`, `.github/renovate.json5`. Also check for `renovate` key in package.json. Return installed if any found.

install(): Config-only, no packages to install.
1. Generate `renovate.json` using `writeConfigFile()`:
```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"]
}
```
Per Claude's Discretion recommendation: `config:recommended` is the official Renovate baseline. It auto-merges patch updates, groups minor updates, and runs weekly. This balances freshness with stability.

2. Return InstallResult with `packagesAdded: []` (config-only module).

**editorconfig.ts**: Create using `defineEnhancement()`.

detect(): Check for `.editorconfig` in project root. Return installed if exists.

install(): Config-only, no packages to install.
1. Generate `.editorconfig` using `writeConfigFile()`:
```ini
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

[Makefile]
indent_style = tab
```
Per Claude's Discretion: 2-space indent (JS/TS ecosystem standard), LF line endings (cross-platform), UTF-8, markdown exception for trailing whitespace, Makefile tab exception.

2. Return InstallResult with `packagesAdded: []` (config-only module).

**Test files**: Follow ci.test.ts pattern (lightweight mocks since these are config-only).

renovate.test.ts:
- detect returns false when no renovate config exists
- detect returns true when renovate.json exists
- detect returns true when package.json has renovate key
- install writes renovate.json with config:recommended
- install returns empty packagesAdded array

editorconfig.test.ts:
- detect returns false when no .editorconfig exists
- detect returns true when .editorconfig exists
- install writes .editorconfig with expected content
- install includes indent_size 2 and indent_style space
- install includes Makefile tab exception
- install includes markdown trim exception
- install returns empty packagesAdded array
  </action>
  <verify>Run `bun run test --filter @tinkerise/core` and verify both test files pass.</verify>
  <done>Renovate generates renovate.json with config:recommended. EditorConfig generates .editorconfig with JS/TS ecosystem defaults. Both are config-only (no package installs). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Register all Phase 9 enhancement modules in barrel and update add command description</name>
  <files>
    packages/core/src/enhancements/modules/index.ts
    packages/cli/src/commands/add.ts
  </files>
  <action>
**IMPORTANT**: This task MUST run AFTER Task 1 within this plan. Plan 09-01 also modifies index.ts (adding docker + env). Since plans 09-01, 09-02, and 09-03 run in parallel (Wave 1), each plan adds its OWN modules to index.ts. The executor for THIS plan should:

1. Read the CURRENT state of index.ts (which may already have docker+env from 09-01 if it finished first, or may not)
2. Add imports and registrations for the modules created in THIS plan (renovate, editorconfig) AND the modules from plan 09-02 (commitlint, testing) — since 09-02 does not modify index.ts itself

Wait — to avoid file conflicts, let me restructure: Plan 09-01 Task 2 already handles adding docker+env to index.ts. This task needs to add ALL FOUR remaining modules (commitlint, testing, renovate, editorconfig) to index.ts.

Actually, re-reading plan 09-01 and 09-02: plan 09-01 Task 2 adds docker+env to index.ts. Plans 09-02 and 09-03 both need to add their modules. Since all three plans run in parallel, the LAST one to finish will overwrite the others.

**Resolution**: This task (09-03 Task 2) will add commitlint, testing, renovate, and editorconfig to index.ts. Plan 09-01 adds docker and env. If this plan finishes after 09-01, we read fresh and add ours. If before, 09-01 will read fresh and add theirs. The key is that each plan reads fresh and only appends — no destructive overwrites.

Update `packages/core/src/enhancements/modules/index.ts`:
- Add export statements for commitlintModule, testingModule, renovateModule, editorconfigModule
- Add imports for all four
- Add all four to the `allEnhancementModules` array (after existing entries)
- The enhancementRegistry Map auto-updates since it reads from allEnhancementModules

Read the current index.ts first, then append the new modules. If docker/env are already there from plan 09-01, keep them. If not, add them too to be safe (the plan 09-01 executor will also add them, resulting in the correct final state).

Update `packages/cli/src/commands/add.ts`:
- Update the `.description()` string to mention all available enhancements: "Add ESLint, Prettier, husky, GitHub Actions CI, Docker, env, commitlint, testing, renovate, EditorConfig, and more to your project."
- Update the `.argument()` help text to list all valid enhancement names

No test needed for index.ts changes — the existing registry tests and the new module tests cover this.
  </action>
  <verify>Run `bun run test --filter @tinkerise/core` to confirm no import/export errors. Run `bun run build` to confirm TypeScript compilation succeeds. Verify enhancementRegistry size includes the new modules.</verify>
  <done>All four new modules (commitlint, testing, renovate, editorconfig) are registered in the enhancement module barrel. The add command description lists all available enhancements. Build succeeds.</done>
</task>

</tasks>

<verification>
- `bun run test --filter @tinkerise/core` passes with all new tests
- Both modules are config-only (no installPackages calls)
- All 10 enhancement modules (4 original + 6 new) registered in enhancementRegistry
- `bun run build` succeeds
</verification>

<success_criteria>
- renovateModule generates renovate.json with config:recommended
- editorconfigModule generates .editorconfig with 2-space indent, LF, Makefile tab exception
- Both return empty packagesAdded
- All Phase 9 enhancement modules registered in barrel export
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-additional-enhancements-utility-templates/09-03-SUMMARY.md`
</output>
