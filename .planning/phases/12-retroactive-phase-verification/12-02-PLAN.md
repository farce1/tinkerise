---
phase: 12-retroactive-phase-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md
autonomous: true
requirements:
  - ADD-01
  - ADD-02
  - ADD-03
  - ADD-04
  - CLI-02

must_haves:
  truths:
    - "Phase 6 VERIFICATION.md exists with frontmatter, summary table, environment metadata, and per-requirement evidence"
    - "All 5 requirements (ADD-01 through ADD-04, CLI-02) have individual sections with PASS, PARTIAL PASS, or FAIL status"
    - "Each requirement cites specific file paths and/or test names as evidence -- no summary-only references"
    - "CLI-02 verification confirms BOTH package.json bin entries AND runtime basename detection logic (research Pitfall 3)"
    - "ADD-01 verification confirms framework-appropriate plugin support via FRAMEWORK_ESLINT_MAP"
    - "No requirement has FAIL status (all pass or partial pass)"
  artifacts:
    - path: ".planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md"
      provides: "Phase 6 verification report for 5 enhancement/CLI requirements"
      contains: "## Summary"
  key_links:
    - from: "06-VERIFICATION.md"
      to: "packages/core/src/enhancements/modules/eslint.ts"
      via: "ADD-01 evidence references"
      pattern: "eslint\\.ts"
    - from: "06-VERIFICATION.md"
      to: "packages/cli/src/index.ts"
      via: "CLI-02 basename detection evidence"
      pattern: "index\\.ts"
---

<objective>
Verify Phase 6 (Core Enhancements & Add Command) requirements and generate 06-VERIFICATION.md

Purpose: Confirm 5 requirements (ADD-01 through ADD-04, CLI-02) are satisfied by tracing each to specific source code, test results, and runtime behavior. This closes the verification gap for the core enhancement modules and tk alias.

Output: `.planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-retroactive-phase-verification/12-RESEARCH.md

Key evidence files to inspect:
- packages/core/src/enhancements/modules/eslint.ts (FRAMEWORK_ESLINT_MAP, eslintModule)
- packages/core/src/enhancements/modules/prettier.ts (prettierModule, hasTailwind check)
- packages/core/src/enhancements/modules/husky.ts (huskyModule, lint-staged config)
- packages/core/src/enhancements/modules/ci.ts (ciModule, PM_CI_MAP, buildCiYaml)
- packages/cli/src/index.ts (lines 35-36: basename detection for tk alias)
- packages/cli/package.json (bin.tk entry)
- packages/tinkerise/package.json (bin.tk entry)

Test files to run:
- packages/core/tests/enhancements/modules/eslint.test.ts
- packages/core/tests/enhancements/modules/prettier.test.ts
- packages/core/tests/enhancements/modules/husky.test.ts
- packages/core/tests/enhancements/modules/ci.test.ts
- packages/cli/tests/commands/add.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 6 tests and inspect all 5 requirement evidence files</name>
  <files>
    packages/core/src/enhancements/modules/eslint.ts
    packages/core/src/enhancements/modules/prettier.ts
    packages/core/src/enhancements/modules/husky.ts
    packages/core/src/enhancements/modules/ci.ts
    packages/cli/src/index.ts
    packages/cli/package.json
    packages/tinkerise/package.json
  </files>
  <action>
    Run the Phase 6 related test suites to confirm current pass status:
    ```bash
    bun run test -- --reporter=verbose packages/core/tests/enhancements/modules/eslint.test.ts packages/core/tests/enhancements/modules/prettier.test.ts packages/core/tests/enhancements/modules/husky.test.ts packages/core/tests/enhancements/modules/ci.test.ts packages/cli/tests/commands/add.test.ts
    ```

    Then inspect each requirement's evidence files:

    **ADD-01 (ESLint flat config with framework plugins):**
    - Confirm `eslintModule` in eslint.ts implements the EnhancementModule interface
    - Confirm `FRAMEWORK_ESLINT_MAP` covers at least: next, react, remix, vue, nuxt, svelte, astro
    - Confirm the install function adapts ESLint config based on detected framework
    - Confirm ESLint tests cover framework-specific plugin scenarios

    **ADD-02 (Prettier with Tailwind auto-detection):**
    - Confirm `prettierModule` in prettier.ts implements EnhancementModule
    - Confirm Tailwind detection logic (checks for tailwindcss in dependencies)
    - Confirm zero-config default (no .prettierrc when no Tailwind)
    - Confirm .prettierrc created only when Tailwind detected
    - Confirm prettier tests cover both Tailwind and non-Tailwind scenarios

    **ADD-03 (Husky + lint-staged for pre-commit):**
    - Confirm `huskyModule` in husky.ts sets up husky and lint-staged
    - Confirm .git check before husky install
    - Confirm adaptive lint-staged config based on installed tools (ESLint, Prettier)
    - Confirm husky tests cover .git guard and adaptive config

    **ADD-04 (GitHub Actions CI workflow):**
    - Confirm `ciModule` in ci.ts generates a CI workflow
    - Confirm `PM_CI_MAP` covers all 4 package managers (npm, pnpm, yarn, bun)
    - Confirm workflow includes lint, type-check, test, build steps (conditional on installed tools)
    - Confirm CI tests verify workflow generation for different PM configs

    **CLI-02 (tk alias):**
    - Confirm `bin.tk` in `packages/cli/package.json`
    - Confirm `bin.tk` in `packages/tinkerise/package.json`
    - Confirm runtime basename detection in `packages/cli/src/index.ts` (lines ~35-36)
    - Confirm programName is dynamically set based on basename (tk vs tinkerise)
    - This has TWO levels: package.json bin AND runtime detection (research Pitfall 3)

    Record all findings with file paths, line numbers, test counts, pass/fail status. Note any gaps.
  </action>
  <verify>All listed test suites pass. Each of the 5 requirements has at least one specific file path + line number or test name as evidence.</verify>
  <done>Complete evidence dossier exists for all 5 Phase 6 requirements, test results captured, any gaps identified.</done>
</task>

<task type="auto">
  <name>Task 2: Write 06-VERIFICATION.md with per-requirement evidence and summary table</name>
  <files>.planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md` following the established template.

    **Structure (locked decisions):**

    1. **Frontmatter:** phase, verified (ISO timestamp), status, score (N/5), re_verification: false

    2. **Header:** Phase goal from roadmap, verified timestamp, status

    3. **Summary table:** Pass / Partial Pass / Fail counts with overall status

    4. **Environment:** Verification date, Node.js version, OS, Bun version, Vitest version

    5. **Requirement Verification:** One section per requirement (ADD-01, ADD-02, ADD-03, ADD-04, CLI-02), each with:
       - **Status:** PASS | PARTIAL PASS | FAIL
       - **Evidence:** Specific file paths, line numbers, test names
       - **Notes:** Any caveats

    6. **Test Results Summary:** Table of test files with counts and status

    7. **Gaps Found:** Any gaps + fixes (or "None")

    8. **Human Verification Required:** Items needing manual testing (or "None")

    **Key formatting rules:**
    - Requirement ID only (ADD-01), not quoted text
    - CLI-02 MUST show evidence for BOTH bin entries AND basename detection
    - ADD-01 MUST reference FRAMEWORK_ESLINT_MAP with framework coverage list
    - ADD-02 MUST show Tailwind detection logic path
    - If any gap is found and fixable, fix it and note in Gaps section

    Commit:
    ```bash
    node ./.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(12-02): add Phase 6 verification report" --files .planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md
    ```
  </action>
  <verify>
    - File exists at `.planning/phases/06-core-enhancements-add-command/06-VERIFICATION.md`
    - Contains frontmatter with phase, verified, status, score fields
    - Contains Summary table
    - Contains Environment section
    - Contains 5 requirement sections (ADD-01 through ADD-04, CLI-02)
    - CLI-02 section references both bin entries and basename detection
    - No requirement has FAIL status
    - File is committed
  </verify>
  <done>Phase 6 VERIFICATION.md exists with all 5 requirements verified to pass or partial-pass status, committed to git.</done>
</task>

</tasks>

<verification>
- 06-VERIFICATION.md exists with valid frontmatter
- Summary table shows 5 requirements all at PASS or PARTIAL PASS
- Each requirement section cites specific source files
- CLI-02 confirms both bin entry and runtime detection
- ADD-01 confirms framework plugin coverage
- Environment metadata is present
- File is committed
</verification>

<success_criteria>
Phase 6 has a VERIFICATION.md confirming ADD-01 through ADD-04, CLI-02 with per-requirement evidence traced to source code and test results. All 5 requirements pass or partial-pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-retroactive-phase-verification/12-02-SUMMARY.md`
</output>
