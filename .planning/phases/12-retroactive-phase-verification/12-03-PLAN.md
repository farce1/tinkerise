---
phase: 12-retroactive-phase-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-distribution-release-automation/10-VERIFICATION.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04
  - DIST-05
  - DIAG-03
  - QA-08

must_haves:
  truths:
    - "Phase 10 VERIFICATION.md exists with frontmatter, summary table, environment metadata, and per-requirement evidence"
    - "All 7 requirements (DIST-01 through DIST-05, DIAG-03, QA-08) have individual sections with PASS, PARTIAL PASS, or FAIL status"
    - "Each requirement cites specific file paths as evidence -- no summary-only references"
    - "DIST-03 and DIST-04 explicitly acknowledge template-complete status and external infrastructure dependency"
    - "DIST-05 and DIAG-03 are verified separately despite sharing source files"
    - "QA-08 verification traces changesets config, release workflow, and version/publish scripts"
    - "No requirement has FAIL status (all pass or partial pass)"
  artifacts:
    - path: ".planning/phases/10-distribution-release-automation/10-VERIFICATION.md"
      provides: "Phase 10 verification report for 7 distribution/release requirements"
      contains: "## Summary"
  key_links:
    - from: "10-VERIFICATION.md"
      to: "packages/tinkerise/package.json"
      via: "DIST-01 and DIST-02 evidence"
      pattern: "package\\.json"
    - from: "10-VERIFICATION.md"
      to: ".github/workflows/release.yml"
      via: "QA-08 and DIST-04 evidence"
      pattern: "release\\.yml"
---

<objective>
Verify Phase 10 (Distribution & Release Automation) requirements and generate 10-VERIFICATION.md

Purpose: Confirm 7 requirements (DIST-01 through DIST-05, DIAG-03, QA-08) are satisfied by tracing each to specific source code, configuration files, and infrastructure templates. This closes the verification gap for distribution, self-update, and release automation.

Output: `.planning/phases/10-distribution-release-automation/10-VERIFICATION.md`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-retroactive-phase-verification/12-RESEARCH.md

Key evidence files to inspect:
- packages/tinkerise/package.json (bin entries, dependencies, publishConfig)
- packages/tinkerise/bin.mjs (shebang + import for npx support)
- packages/cli/package.json (publishConfig, bin entries)
- packages/core/package.json (publishConfig)
- packages/shared/package.json (publishConfig)
- packages/cli/src/utils/install-method.ts (detectInstallMethod)
- packages/cli/src/commands/update.ts (registerUpdateCommand, method-aware switch)
- packages/cli/src/utils/update-check.ts (background update check, 24h cache, nudge)
- homebrew/tinkerise.rb (Homebrew formula template)
- homebrew/update-formula.yml (workflow_dispatch automation for tap)
- .github/workflows/release.yml (changesets/action, npm publish, Homebrew trigger)
- .changeset/config.json (fixed versioning)
- package.json (root: changeset, ci:version, ci:publish scripts)

NOTE: Phase 10 has NO dedicated test files (research finding). Verification relies on code inspection and configuration review, not test results. This is expected and documented -- Phase 10 is primarily infrastructure code.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect all 7 Phase 10 requirement evidence files</name>
  <files>
    packages/tinkerise/package.json
    packages/tinkerise/bin.mjs
    packages/cli/package.json
    packages/core/package.json
    packages/shared/package.json
    packages/cli/src/utils/install-method.ts
    packages/cli/src/commands/update.ts
    packages/cli/src/utils/update-check.ts
    homebrew/tinkerise.rb
    homebrew/update-formula.yml
    .github/workflows/release.yml
    .changeset/config.json
    package.json
  </files>
  <action>
    No dedicated test suites to run for Phase 10 (infrastructure code). Perform thorough code inspection for each requirement.

    **DIST-01 (npm published with tinkerise and tk bin entries):**
    - Confirm `packages/tinkerise/package.json` has `bin: { tinkerise: './bin.mjs', tk: './bin.mjs' }`
    - Confirm `packages/cli/package.json` has `publishConfig.access: 'public'`
    - Confirm `packages/core/package.json` has `publishConfig.access: 'public'`
    - Confirm `packages/shared/package.json` has `publishConfig.access: 'public'`
    - All 3 scoped packages need public publish access for npm

    **DIST-02 (npx tinkerise works without global install):**
    - Confirm `packages/tinkerise/package.json` is the unscoped wrapper package
    - Confirm `packages/tinkerise/bin.mjs` has shebang and imports `@tinkerise/cli`
    - Confirm no build step needed (plain .mjs files)
    - Confirm dependencies include `@tinkerise/cli: workspace:*`

    **DIST-03 (Homebrew tap provides brew install tinkerise):**
    - Confirm `homebrew/tinkerise.rb` exists with complete formula (install, caveats, test blocks)
    - Note: Template-complete but depends on external tap repo (research Pitfall 4)
    - Document what remains for full deployment

    **DIST-04 (Homebrew formula auto-update via GH Actions):**
    - Confirm `homebrew/update-formula.yml` exists with workflow_dispatch trigger
    - Confirm `.github/workflows/release.yml` has a step that triggers the tap update
    - Check the trigger condition (`changesets.outputs.published == 'true'`)
    - Note: Automation template is complete; requires HOMEBREW_TAP_TOKEN secret and tap repo

    **DIST-05 (Install-method detection and update instructions):**
    - Confirm `detectInstallMethod()` in install-method.ts identifies: homebrew, npm-global, npx, unknown
    - Confirm detection logic checks Homebrew prefix, npm global prefix, and npm_execpath
    - Confirm the function is exported and consumed by update.ts

    **DIAG-03 (tinkerise update with install-method-aware instructions):**
    - Confirm `registerUpdateCommand()` in update.ts has switch/if on method type
    - Confirm each method gets appropriate update instructions (brew upgrade, npm update -g, npx info)
    - Confirm background update check in update-check.ts with 24h cache and nudge display
    - Verify DIST-05 and DIAG-03 SEPARATELY despite shared source files (research note)

    **QA-08 (Release automation via changesets + GitHub Actions):**
    - Confirm `.changeset/config.json` exists with fixed versioning config
    - Confirm `.github/workflows/release.yml` uses `changesets/action` for Version PRs and publish
    - Confirm root `package.json` has `changeset`, `ci:version`, `ci:publish` scripts
    - Check that fixed versioning lists all 4 packages explicitly

    Record all findings with exact file paths, line numbers, configuration values. Note any gaps.
  </action>
  <verify>Each of the 7 requirements has at least one specific file path + configuration value or code reference as evidence.</verify>
  <done>Complete evidence dossier exists for all 7 Phase 10 requirements, any gaps identified.</done>
</task>

<task type="auto">
  <name>Task 2: Write 10-VERIFICATION.md with per-requirement evidence and summary table</name>
  <files>.planning/phases/10-distribution-release-automation/10-VERIFICATION.md</files>
  <action>
    Create `.planning/phases/10-distribution-release-automation/10-VERIFICATION.md` following the established template.

    **Structure (locked decisions):**

    1. **Frontmatter:** phase, verified (ISO timestamp), status, score (N/7), re_verification: false

    2. **Header:** Phase goal from roadmap, verified timestamp, status

    3. **Summary table:** Pass / Partial Pass / Fail counts with overall status

    4. **Environment:** Verification date, Node.js version, OS, Bun version

    5. **Requirement Verification:** One section per requirement (DIST-01, DIST-02, DIST-03, DIST-04, DIST-05, DIAG-03, QA-08), each with:
       - **Status:** PASS | PARTIAL PASS | FAIL
       - **Evidence:** Specific file paths, configuration values, code references
       - **Notes:** Any caveats (especially for DIST-03 and DIST-04 regarding external infra)

    6. **Test Results Summary:** Note that Phase 10 has no dedicated test files. This section should state: "Phase 10 features are infrastructure/configuration code with no dedicated test suites. Verification is based on code and configuration inspection."

    7. **Gaps Found:** Any gaps + fixes (or "None")

    8. **Human Verification Required:** Items needing manual testing

    **Key formatting rules:**
    - Requirement ID only (DIST-01), not quoted text
    - DIST-03 and DIST-04 MUST explicitly note "template-complete, external infrastructure required for deployment" -- do NOT rubber-stamp as full PASS without acknowledging this (research Pitfall 4)
    - DIST-05 and DIAG-03 MUST be separate sections despite referencing some of the same source files
    - QA-08 must trace through changeset config, release workflow, AND root package.json scripts
    - If any gap is found and fixable, fix it and note in Gaps section

    Commit:
    ```bash
    node ./.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(12-03): add Phase 10 verification report" --files .planning/phases/10-distribution-release-automation/10-VERIFICATION.md
    ```
  </action>
  <verify>
    - File exists at `.planning/phases/10-distribution-release-automation/10-VERIFICATION.md`
    - Contains frontmatter with phase, verified, status, score fields
    - Contains Summary table
    - Contains Environment section
    - Contains 7 requirement sections (DIST-01 through DIST-05, DIAG-03, QA-08)
    - DIST-03 and DIST-04 notes acknowledge external infrastructure dependency
    - DIST-05 and DIAG-03 are separate sections
    - No requirement has FAIL status
    - File is committed
  </verify>
  <done>Phase 10 VERIFICATION.md exists with all 7 requirements verified to pass or partial-pass status, committed to git.</done>
</task>

</tasks>

<verification>
- 10-VERIFICATION.md exists with valid frontmatter
- Summary table shows 7 requirements all at PASS or PARTIAL PASS
- Each requirement section cites specific source files or config values
- DIST-03/DIST-04 acknowledge template-complete external dependency
- DIST-05 and DIAG-03 verified separately
- QA-08 traces full release pipeline
- File is committed
</verification>

<success_criteria>
Phase 10 has a VERIFICATION.md confirming DIST-01 through DIST-05, DIAG-03, QA-08 with per-requirement evidence traced to source code and configuration. All 7 requirements pass or partial-pass.
</success_criteria>

<output>
After completion, create `.planning/phases/12-retroactive-phase-verification/12-03-SUMMARY.md`
</output>
